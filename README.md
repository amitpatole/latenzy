# latenzy

Per-model LLM latency monitoring for enterprises — a synthetic prober and Prometheus
exporter with prebuilt Grafana dashboards, comparing TTFT, total latency, throughput,
and rate-limit pressure across Claude, OpenAI, and Gemini models (model A vs model B,
not lab-level rollups).

> **Status:** name reservation placeholder (`0.0.0`). First real release coming soon.

— amitpatole
