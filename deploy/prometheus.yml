global:
  scrape_interval: 60s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: latenzy
    authorization:
      type: Bearer
      credentials_file: /run/secrets/latenzy_token
    static_configs:
      - targets: ["latenzy:9877"]
